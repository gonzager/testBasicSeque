const autorSchema = require('./autor.schema')
const postSchema = require('./post.schema')
const comentarioSchema = require('./comentario.schema')

module.exports = {autorSchema, postSchema,comentarioSchema}